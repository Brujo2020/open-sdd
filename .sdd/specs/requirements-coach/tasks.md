# Implementation Plan — requirements-coach

## Tasks

- [x] 1. The deterministic check runner — _Requirements: REQ-RQC-001, REQ-RQC-003_ — _Boundary:_ `tools/open-sdd/src/core/requirementsCoach.ts`, `tools/open-sdd/test/coreRequirementsCoach.test.ts`
  - _Evidence: `reviewRequirements({ feature, artifacts, entries, runner })` returns findings with check id, severity, `file:line:column`, span where available, basis and a graded remedy or a question; every finding is attributed to its instrument. `test/coreRequirementsCoach.test.ts` 41 tests green._
- [x] 2. Seed the eight families from the stored catalogue — _Requirements: REQ-RQC-001_ — _Boundary:_ `.sdd/settings/standards/**`, `tools/open-sdd/test/coreRequirementsFamilies.test.ts`
  - _Evidence: 31 check ids implemented with the document's tiers (the 29 required plus `AMB-005` and `SET-003`); a same-id catalogue entry reclassifies the severity while `defaultSeverity` keeps the document tier visible. Deliberately not hard-coded: the other 11 catalogue ids run through the injected `runner` and are attributed `instrument: 'catalogue'`, so built-in ids are never re-run and duplicated._
- [x] 3. Findings never dead-end — _Requirements: REQ-RQC-003, REQ-RQC-006_ — _Boundary:_ `tools/open-sdd/src/core/requirementsCoach.ts`, `tools/open-sdd/test/coreRequirementsDeadEnds.test.ts`
  - _Evidence: the four abstention triggers (undefined actor, undecided threshold, two-way term, doubtful necessity) emit question-only findings, and `reviewRequirements` normalises any finding that somehow arrives without a remedy into an explicit `needs-human` remedy. `test/coreRequirementsDeadEnds.test.ts` 4 tests green._
- [x] 4. Governance split: deterministic gates, model advises — _Requirements: REQ-RQC-002, REQ-RQC-011_ — _Boundary:_ `tools/open-sdd/src/core/requirementsCoach.ts`, `tools/open-sdd/src/core/gateRunner.ts`, `tools/open-sdd/test/coreRequirementsAuthority.test.ts`
  - _Evidence: `mayBlock` is true only for deterministic error/warning findings; `asModelFinding` forces a model finding to `mayBlock: false`; the review header prints "this reports checks and adjudication, not correctness" and `requirements review standards-engine` shows "0 escalated to a human" on a spec with no injection._
- [ ] 5. Calibration harness and reported metrics — _Requirements: REQ-RQC-004_ — _Boundary:_ `tools/open-sdd/bench/**`, `docs/MEASUREMENTS.md`, `tools/open-sdd/test/coreRequirementsCalibration.test.ts`
  - Not done in this slice: there is no labelled defect corpus yet, so per-family recall and false-positive rate are not measured and no check may be promoted to blocking.
- [x] 6. Executable checklists — _Requirements: REQ-RQC-005_ — _Boundary:_ `tools/open-sdd/src/core/checklist.ts`, `tools/open-sdd/templates/specs/checklist.md`, `tools/open-sdd/test/coreChecklist.test.ts`
  - _Evidence: `parseChecklist` reads `cmd`/`artifact`/`property`/`trace` predicates; `verifyChecklist` executes them and returns patched markdown reusing `EVIDENCE_MARKER`/`checkEvidenceLock` from `core/triad.ts`; a `[x]` without a digest is a failure. `test/coreChecklist.test.ts` 12 tests green._
- [x] 7. Non-functional requirement shape — _Requirements: REQ-RQC-007_ — _Boundary:_ `.sdd/settings/standards/**`, `tools/open-sdd/test/coreNfrShape.test.ts`
  - _Evidence: `NFR-002`/`NFR-003` require a percentile and a measurement window and treat an absolute target with no error budget as advisory; covered in `test/coreRequirementsCoach.test.ts`._
- [x] 8. Traceability findings — _Requirements: REQ-RQC-008_ — _Boundary:_ `tools/open-sdd/src/core/specConstitution.ts`, `tools/open-sdd/src/core/reuseFirst.ts`, `tools/open-sdd/test/coreTraceability.test.ts`
  - _Evidence: `TRC-002` reports a dangling trace reference as S1 (`requirements review standards-engine` shows `TRC-002 … does not resolve`); `SET-005`/`SET-006` report an orphan requirement and a requirement with no downstream evidence, and go to `skipped` with a reason when the link source is not among the artifacts rather than passing._
- [x] 9. The one-page review surface — _Requirements: REQ-RQC-009_ — _Boundary:_ `tools/open-sdd/src/core/specReview.ts`, `tools/open-sdd/src/cli/commands/requirements.ts`, `tools/open-sdd/test/coreSpecReview.test.ts`
  - _Evidence: `reviewSpec` returns added/modified/removed rows with `wordDiff`, tasks, failing tests and a risk score with named factors, and `exitCode: 1` on an unapproved change; the CLI reads the base with `git show <ref>:<path>` and approvals from `spec.json`. `test/coreSpecReview.test.ts` 8 tests green._
- [x] 10. Injection handling and the local backend boundary — _Requirements: REQ-RQC-010_ — _Boundary:_ `tools/open-sdd/src/core/requirementsCoach.ts`, `.sdd/settings/standards/**`, `tools/open-sdd/test/coreRequirementsInjection.test.ts`
  - _Evidence: `AI-002` flags instruction-shaped spec content and sets `escalate: true` with a `needs-human` remedy plus a question; `UNTRUSTED_SPEC_POLICY` states the containment and the core never imports the model client. `test/coreRequirementsCoach.test.ts` exercises both the literal and the invisible-character case._
- [x] 11. The console — _Requirements: REQ-RQC-003, REQ-RQC-009_ — _Boundary:_ `tools/open-sdd/src/cli/commands/requirements.ts`, `tools/open-sdd/test/cliRequirements.test.ts`
  - _Evidence: `requirements review|checklist|fix` and `review --base` are routed from `src/index.ts`, all with `--json` through the existing envelope; exit 2 usage / 1 failed / 3 environment. `requirements review standards-engine` runs 32 checks, reports 34 findings and 1 uninspected._
- [x] 12. Locale contract — _Requirements: REQ-RQC-012_ — _Boundary:_ `tools/open-sdd/src/cli/i18n.ts`, `tools/open-sdd/src/cli/commands/requirements.ts`, `tools/open-sdd/test/cliLocaleRefusal.test.ts`
  - _Evidence: check ids, `REQ-<AREA>-<NNN>`, JSON keys and fix commands stay English under every locale; `status --lang ja` and the requirements console both refuse an untranslated locale by name and list `es`, `en`, exiting 2 and 1 respectively. `test/cliLocale.test.ts` and `test/cliExitCodes.test.ts` green._
- [ ] 13. Teach the coach and register the claim — _Requirements: REQ-RQC-003, REQ-RQC-004_ — _Boundary:_ `tools/open-sdd/templates/agents/_shared/skills/sdd-spec-requirements/SKILL.md`, `docs/guides/requirements-quality-catalog.md`, `docs/claims/paper-claims.yaml`, `README.md`, `.sdd/specs/**`, `docs/EVOLUTION-PLAN.md`
  - Not done in this slice: the skill does not yet teach the coach, and the catalogue still declares itself research input.
